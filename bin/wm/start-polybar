#!/usr/bin/sh

killall -q polybar

while pgrep -u "$(id -u)" -x polybar >/dev/null; do :; done

for m in $(polybar --list-monitors | cut -d":" -f1); do
  MONITOR=$m polybar --reload mainbar-bspwm &
done
